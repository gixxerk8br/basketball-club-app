# 志免南ワイルドキャッツ 活動管理アプリ 仕様書

最終更新: 2026-08-28(配車の個人単位化・スポット同乗者・複数選択割当、家庭の利用停止・完全削除、選手の在籍状態、ライブ配信の権限化、スケジュール資料添付を追記)
作成目的: ChatGPT等の他AI/他者に相談する際に、現状の仕様を正確に共有するための資料。

---

## 1. 概要

- **名称**: WILD CATS活動管理アプリ(プロトタイプ)
- **対象**: 少年バスケットボールクラブ「志免南ワイルドキャッツ」の保護者・指導者向け活動管理ツール
- **目的**: LINEグループの代替として、練習・試合の予定共有、出欠確認、配車調整、大会結果・ライブ配信、会費管理、部活規約の閲覧などを一元化する
- **利用イメージ**: 各家庭がスマートフォン(主にiPhone/Android)で閲覧・入力。ベンチでの試合中スタッツ記録はiPadでの利用を想定。

## 2. 技術構成

- **形式**: 単一HTMLファイル(`index.html`)によるフロントエンドのみのプロトタイプ。ビルド不要。
- **使用ライブラリ**(すべてCDN経由、npm等のインストール不要):
  - [Alpine.js](https://alpinejs.dev/) 3系 — 画面全体の状態管理・DOM操作(`x-data="app()"`)
  - [Tailwind CSS](https://tailwindcss.com/) — スタイリング(CDN JIT版)
  - [Chart.js](https://www.chartjs.org/) 4系 — 選手成績グラフ
- **サーバー/バックエンド**: 現時点では**なし**。`npx http-server` 等の静的ファイルサーバーで配信しているのみ。
- **データ保存**: ブラウザの `localStorage` のみ(キー: `bbclub_prototype_data`)。サーバーには一切送信されない。
  - 同じ端末・同じブラウザでしかデータは共有されない(=現状は本番運用不可。家族間でのデータ共有ができないプロトタイプ段階)。
  - 管理画面に「全データリセット」ボタンあり(誤操作対策で確認ダイアログ付き)。
- **認証**: 現状は本物の認証ではなく、`members` 配列に事前登録されたメールアドレス+簡易パスワードでのモック認証(ローカルの配列と突き合わせるだけ)。

### 今後の想定(未着手・検討中)

- Supabase(PostgreSQL + Auth + Row Level Security)への移行を検討中だが、**内容を精査してから着手する方針**でまだ未着手。
- 本番移行時は以下が必須になる想定(ユーザーのセキュリティ方針より):
  - 本物の認証・認可、Supabase RLSによるデータアクセス制御
  - 個人情報の最小化(生年月日など不要な個人情報は持たない/カレンダーからも除外)
  - 写真・動画は自前ホスティングせず、YouTube等のメタデータ(URLのみ)を保存する方針
  - APIキー等の秘密情報のハードコード禁止、無料枠内でのコスト管理

## 3. ユーザー・権限モデル

`members` 配列がログインアカウント。1メンバー = 原則1家庭(保護者)で、配下に `familyMembers[]`(子ども・きょうだいなど)を持つ。

| 権限フラグ | 意味 |
|---|---|
| `role === 'admin'` | 管理者。全機能にアクセス可能 |
| `canManageCarpool` | 配車管理者。管理者でなくても配車の組み立てが可能 |
| `canManageFinance` | 会計担当。管理者でなくても会費タブの管理が可能 |
| `canManageLive` | 配信管理者。管理者でなくても動画・ライブ配信URLの登録・編集・削除・配信開始/終了が可能 |
| (それ以外の一般会員) | 閲覧・自分の出欠回答・配車回答が可能。ライブ配信の閲覧は誰でもできるが、投稿・操作は上記の権限を持つ人のみ |

**設計方針として特筆すべき点**:
- ライブ配信の投稿・編集・削除・配信開始/終了は「配信管理者」権限(`canManageLive`、管理者は自動的に保有)を持つ人のみ操作できる。配車管理者・会計担当と同じ「基本ロール+追加権限」の考え方で、管理者ダッシュボードの🔑役割・権限の設定から、家庭ごとに個別に付与できる(2026-08-28、ユーザーの明示的な指示により「誰でも操作可」から変更。BANDアプリの投稿権限モデルを参考にした)。閲覧(視聴)自体はログイン済みの会員なら誰でも可能。
- 配車の組み立て(誰をどの車に乗せるか)は「配車管理者」権限が必要(管理者 or `canManageCarpool`)。

## 4. データモデル(主要エンティティ)

```
members[]          … ログインアカウント(保護者)。familyMembers[] を内包
players[]          … スタッツ記録用の選手ロースター
scheduleDays[]      … カレンダー1日=1エントリ。dayType(練習/試合/OFF等)を1つ持つ
  └ opponents[]      … その日が試合の場合の対戦相手リスト。各要素が games[] とgameIdで紐付く
games[]             … 対戦相手ごとの試合記録(スタッツ・結果・動画/ライブ配信URL)
announcements[]     … お知らせ(カテゴリタグ、任意リンク付き)
financeEntries[]    … 会費関連の入出金記録
bylawsArticles[]    … クラブ規約(12条、PDFから転記)
carpoolResponses{}  … 日付ごとの配車希望回答 { memberId: {status, capacity, riders:[familyMemberId,...]} }
                       riders は「実際に参加する家族メンバーのID一覧」(選手・家族同乗者を問わず個人単位)
carpoolAssignments{}… 日付ごとの配車割当 { 乗車者(familyMemberId): 運転者のmemberId }(1人は必ず1台にしか乗らない)
```

### 配車は「乗車者(個人)単位」で管理する

配車は家庭ごとの人数(headcount)ではなく、選手・家族同乗者を含めた**個人単位**で管理する。`familyMembers`(選手・保護者・きょうだいなど)のうち、クラブに所属しない家族同乗者(弟妹など)も含めて、実際に乗る人を1人ずつ選んで車に割り当てる。UI上は家庭ごとにまとめて表示し、タップ操作(①乗せたい人を選ぶ→②車を選ぶ)だけで組み立てられるようにしている。

### スケジュールの考え方

- 以前は「1日に複数イベント」を許容するモデルだったが、**現在は「1日=1種類の活動(dayType)」に単純化**されている。
- 練習日は「休む人だけ申告する」欠席制(全員参加が前提、Yes/No/未定の3択ではない)。
- 試合日は「参加/不参加/未定」の3択+ 配車希望の回答が必要。

## 5. 主な機能一覧

### 5.1 ホーム画面
- お知らせ一覧(カテゴリタグ、任意リンク付き)
- クイックアクセス: 「🎥動画・ライブ配信」「📜クラブ規約」

### 5.2 スケジュール(カレンダー)
- 月間カレンダー表示。日付ごとに練習/試合/OFF等を色分け表示
- 練習日: 欠席のみ申告(`toggleMyAbsence`)、欠席者一覧を表示
- 試合日: 出欠3択の回答、配車状況をインラインで確認可能
- **資料の複数添付**(管理者が日程ごとに設定): 大会要項・配車表などをGoogleドライブ等にアップロードし、そのリンクを日程1件につき複数登録できる。ファイルそのものはアプリ側に保存せず、リンク(URL)のみを保持する方針(動画・写真と同じ考え方)。

### 5.3 配車管理(乗車者=個人単位で再設計済み)
- 各家庭が配車ステータスを回答: `配車可能` / `配車可能(別便可)` / `別便希望` / `配車不可` / `不参加`
- 配車可否とあわせて、「実際に参加する家族」を個人単位でタップ選択する(選手だけでなく、クラブに所属しない家族同乗者(きょうだい等)も選べる)
- **配車管理者向けの組み立てUI**(2タップ方式、ドラッグ&ドロップではなくタップ操作でモバイルでも確実に動作するよう設計):
  1. 「乗せたい人」を家庭ごとにまとめて表示したチップから、個人単位で**複数人まとめて**タップして選択できる
  2. 割り当てたい車のカードをタップ → 選択していた全員がまとめてその車に割り当てられる
  3. 1人は必ずどこか1台にしか乗らないため、同じ家庭の中でも選手だけ別の車、きょうだいは別の車、といった個人単位の分割が自然にできる(実機検証済み)
  6. 運転者が回答した「乗車可能人数」を超える割り当てはできない(満席の車をタップしても乗せられず、別の車を選ぶよう案内される)
- **サインアップ(新規登録)時の家族情報登録を「保護者/選手/家族同乗者(クラブ非所属)」の3カテゴリに分けて入力できる**。データ構造(`familyMembers[].relation`)自体は変えず、入力フォームの見せ方だけを整理したもの。
- **スポット同乗者(イレギュラー対応)**: 事前登録された家族以外の方(例:友人の子)を、特定の日程だけ一時的に配車の乗車者として追加できる。氏名のみを日程(`scheduleDay.carpoolSpotRiders[]`)に保存し、個人単位の配車の仕組みにそのまま乗せられる(乗車可能人数の制限も同様に適用される)。
  4. 各車のカードに「今誰が乗っているか」が座席表のように、乗車者名+家庭名で常時表示される(視覚的にわかりやすい)
  5. 割り当て済みの人を車カードから✕タップで解除可能

### 5.4 試合結果・スタッツ
- iPad想定のライブスタッツ記録(2ステップ: 日付選択→対戦相手選択)
- 「試合を終了する/再開する」ワークフロー
- ボタンラベルは専門用語を避けた平易な表現
- 選手ごとの成績比較・詳細グラフ(Chart.js)
- **見た目は市販のバスケ専用スタッツアプリ(バスケプラス等)風の「電光掲示板・スコアラーズテーブル」デザインに寄せている**(データ項目・記録ロジックは変更なし):
  - スコアボードはダーク背景(HOME/AWAYラベル・大きな得点数字・自チーム白/相手赤)
  - クォーター選択はダーク背景のセグメントコントロール
  - 選手選択はゼッケン番号を模した丸ボタン
  - クォーター別得点・個人スタッツの表は、ダークヘッダー行+縞模様+等幅数字の「ボックススコア」調

### 5.5 動画・ライブ配信(`isLiveManager` のみ操作可・閲覧は全会員可)
- 試合ごとに動画URL・ライブ配信URLを登録可能
- 登録・編集・削除・配信開始/終了は「配信管理者」権限(`canManageLive`。管理者は自動的に保有)を持つ人のみ。管理者ダッシュボードで家庭ごとに個別付与できる
- 視聴(閲覧)自体はログイン済みの会員なら誰でも可能

### 5.6 会費・会計(`isFinanceManager` のみ利用可)
- 会員ごとの会費支払い状況一覧、支払い済み/未払いの切り替え

### 5.7 クラブ規約
- 規約PDFの内容をアプリ内に12条分、テキストとして埋め込み表示

### 5.8 管理者機能
- 全データリセット(誤操作防止の確認ダイアログ付き、非エンジニアでもスマホから安全に初期化できるように配慮)
- **選手ロスターの在籍状態管理**: 選手ごとに「在籍中 / 卒部 / 退部」を切り替えられる。卒部・退部にしても選手データ自体は削除せず、過去の試合・スタッツとの紐付けを壊さない。試合の記録が残っている選手は完全削除できない仕組みにしている(誤ってスタッツを壊せないようにするため)。ロスターからの完全削除は、記録が一切ない選手(登録ミスなど)のみ可能。
- **家庭単位の利用停止・完全削除**(退部・卒部・規約違反など):
  - 「利用停止」: 該当家庭のログインをブロックする(過去の試合・出欠・配車・会費等の記録は一切削除しない)。ログイン時・アプリ起動時の両方でチェックしている。
  - 「完全削除」: ログイン情報(メールアドレス・パスワード等)そのものを取り消せない形で削除する。利用停止とは別の操作とし、二重確認で誤操作を防止している。この操作でも過去の試合・スタッツ等の記録は削除されない。
  - **現状はクライアント側(ブラウザのJavaScript)でのチェックに留まる**。本番運用でSupabase等のバックエンドへ移行する際は、認証・API・データベース側でも同様の拒否を必ず実装する(クライアント側のチェックだけでは、ブラウザの開発者ツール等で回避され得るため)。

## 6. UI/UX上の設計方針

- スマートフォン利用が前提。管理者専用タブはモバイルナビの下部から外し、ヘッダーの⚙️アイコンに集約
- ドロップダウン(`<select>`)は誤操作・タップ漏れの温床になった実績があるため、**配車の人数指定は排除しタップのみで完結する設計に統一**
- 状態遷移は基本的に「1タップ=1つの意味のある操作」になるよう設計(例: 配車は1タップ=1人)

## 7. 既知の制約・今後の課題

- **データが端末のlocalStorageにしか無いため、家族間でリアルタイムに共有されない**(最大の制約。本番運用にはバックエンド移行が必須)
- 認証がモック(パスワードの暗号化・セッション管理なし)
- オフライン専用、複数管理者による同時編集の競合制御なし
- Supabase移行時に必要な設計(認証・RLS・秘密情報管理・個人情報最小化・写真動画のYouTube委任)は方針決定済みだが未着手
- **家庭単位の「利用停止」「完全削除」は、現状クライアント側のチェックのみ**(ログイン時・アプリ起動時に判定)。Supabase移行時は、データベース側(RLS)・API側でも必ず同様の制御を行う必要がある
- **「家庭」と「ログインユーザー(保護者)」はまだ同一概念**(1家庭=1ログイン)。保護者ごとに別々のログインを持てるようにする場合は、家庭とログインユーザーを別のテーブルとして設計し直す必要がある(詳細は `SPEC_REVIEW_2026-08.md` を参照)
- **選手の年度・学年・在籍期間の管理は未実装**。在籍状態(在籍中/卒部/退部)のみ対応済みで、年度更新に伴う一括進級などの自動化は今後の課題

## 8. これまでの主な意思決定の経緯(相談時の背景情報)

- カレンダー取込時、個人のプライバシー保護のため誕生日等の個人情報は除外する方針を最初から徹底
- 配車は「対戦相手ごと」ではなく「日単位・個人単位(選手・家族同乗者・スポット同乗者)」で管理する方式に変更済み(1家族の中でも複数の車に分割割当できる必要があったため)
- ライブ配信機能は当初「管理者限定にしない(誰でも操作可)」という意図的な設計だったが、2026-08-28にユーザーの明確な要望により「配信管理者」権限による制御(BANDアプリの投稿権限モデルを参考)へ変更
- 会費・練習出欠なども含め、全体として「LINEグループの代替」として使えることを目指している

---

以上。ご相談の際は、この仕様書をベースに「次にどの機能を優先すべきか」「Supabase移行の進め方」などをご検討いただけます。
