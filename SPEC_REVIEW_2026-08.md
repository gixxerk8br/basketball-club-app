# 本番運用に向けた仕様レビュー(2026-08-27)

作成目的: ユーザーから提示された「本番運用を見据えた追加仕様」を、現在の `SPEC.md` および `index.html` の実装と照合し、矛盾点・不足点を整理する。

**このドキュメントは分析・提案のみ。コード変更・DB変更は未実施。**
`SPEC.md` 本体への反映は、この内容をユーザーが確認したあとに行う。

---

## 0. 確認した現状(要約)

### 現在のコード構造(`index.html`)

- `members[]`(ログインアカウント)= 実質「1家庭 = 1ログイン」。
  - `familyMembers[]` を内包し、`relation`(父/母/選手/兄/姉/弟/妹)というラベル付き配列で家族を表現。
  - 「選手」のみ `number`(背番号)を持ち、サインアップ時に自動で `players[]` にもコピーされる。
  - **`familyMembers` の選手データと `players[]` は id で紐付いていない**(名前ベースの一度きりのコピー)。将来同姓同名や改名があると食い違うリスクがすでに存在する。
- 配車は「家庭単位・人数(headcount)単位」で管理。
  - `carpoolResponses{ memberId: {status, headcount, capacity} }` … 家庭ごとに1回答、人数は合計数のみ。**誰が乗るかという個人単位の情報はない**。
  - `carpoolAssignments{ riderId(=memberId): [{driverId, count}] }` … 家庭を車に何人割り当てたかという「人数の分割」であり、個人の乗車者IDではない。
- 権限は `role`(`'admin' | 'member'` の2値)+ 追加権限フラグ2つ(`canManageCarpool`, `canManageFinance`)。
- `status` は `'pending' | 'approved'` の2値のみ。**「利用停止」「退部」「卒部」に相当する状態はまだ存在しない**。
- 承認待ちメンバーの「却下」だけがハード削除(`this.members = this.members.filter(...)`)。承認済み家庭を削除・停止する機能は存在しない。
- ライブ配信(`liveUrl`)・動画(`videoUrl`)・写真(`photoUrl`)は**URL文字列のみ保存**しており、動画ファイル自体は保存していない(既存方針と合致)。
- 認証は `members` 配列とのメールアドレス+平文パスワード突き合わせのみの**モック認証**。サーバーもAPIもDBもまだ存在しないため、「サーバー側でのアクセス拒否」は現状のアーキテクチャ上まだ実現不可能(Supabase移行が前提)。
- クラブは1つだけを前提にしたコードで、`club` に相当する概念・IDはどこにも存在しない。

---

## 1. 新仕様との照合(章ごと)

### 1-1. 家庭・保護者・選手の基本構造

| 項目 | 現状 | 新仕様との関係 |
|---|---|---|
| 1家庭に複数の選手 | ✅ 対応済み(`familyMembers` に複数の `選手` を追加可能) | 一致 |
| 1家庭に複数の保護者(データとして) | △ `familyMembers` に父・母を複数登録可能 | 一致(データ上は) |
| 保護者ごとに別ログイン | ❌ 未対応。1家庭=1ログイン(1メール+1パスワード)固定 | **矛盾**。将来「父も母も別々にログイン」するには、ログインアカウントを「家庭」から独立させ、`家庭:ログインユーザー = 1:多` にする必要がある |
| 選手本人のログインを必須にしない | ✅ 現状も選手はログインアカウントを持たない | 一致 |
| 「家庭」「保護者」「選手」「ログインユーザー」を別概念にする | ❌ 現状は `members`(ログイン)の中に家族情報が同居しており、実質「ログインユーザー = 家庭」になっている | **矛盾**。概念の分離が必要 |

### 1-2. 家族同乗者

| 項目 | 現状 |
|---|---|
| クラブに所属しない同乗者という概念 | ❌ 存在しない。`familyMembers` の「弟/妹/兄/姉」は名前とラベルがあるだけで、年齢・続柄などの情報も、配車で個別に選択する仕組みもない |
| 選手と家族同乗者を別概念にする | ❌ 現状は同じ `familyMembers` 配列内で `relation` ラベルが違うだけ |
| 家族同乗者が配車で1人分の枠を使う | ❌ 現状の配車は「家庭ごとの合計人数」でしか数えないため、"誰が" 乗るかを表現できない |

### 1-3. 配車設計(乗車者単位)

| 項目 | 現状 |
|---|---|
| 乗車者単位(個人単位)でDB管理 | ❌ 現状は「家庭単位の人数(headcount)」+「家庭単位の人数分割(count)」。個人を指す `riderId` は実は "家庭ID" であり、個人IDではない |
| UIでは家庭ごとにまとめて選択 | ✅ 考え方としては現状のUI(2タップ方式)に近いが、内部データが人数ベースなので個人単位の選択・解除はできない |
| 自分の子どもの1人だけ乗せる/家族同乗者も乗せる、などの組み合わせ | ❌ 現状は人数の増減しかできず、「誰を」「誰と」乗せるかを区別できない |

### 1-4. ユーザー権限

| 項目 | 現状 |
|---|---|
| 基本ロール(管理者/コーチ/保護者/選手) | △ 現状は `role: 'admin' | 'member'` の2値のみ。「コーチ」「選手」というロール値は存在しない |
| 追加権限という構造(配車管理・会計管理) | ✅ **既に実装済み**。`canManageCarpool` / `canManageFinance` というフラグ+ `isCarpoolManager` / `isFinanceManager` という判定が、まさに「基本ロール+追加権限」の考え方そのもの |
| 追加権限:動画・ライブ配信管理 | ❌ 現状は逆に**全ログイン済みメンバーに常時許可**している(過去にユーザーから明示的に要望された意図的な設計、`CLAUDE.md` に明記) |

⚠️ **重要な矛盾点**: 「ライブ配信は管理者権限不要で誰でも操作できる」という**既存の意図的な仕様**と、今回の「動画・ライブ配信管理は追加権限で管理者以外の特定ユーザーに付与する」という**新しい要望**は、方向性が逆です。全員に開放したままにするか、追加権限フラグ(例: `canManageLive`)を新設して絞るか、どちらにするかはユーザーの判断が必要なため、**ここは変更せずユーザーに確認**します。

### 1-5. アカウント停止・退部・卒部/完全削除の分離/在籍状態・年度

| 項目 | 現状 |
|---|---|
| 家庭単位のアクセス停止 | ❌ 存在しない。`status` は `pending/approved` のみ |
| サーバー側での停止ユーザー拒否 | ❌ 実現不可能(現状サーバー自体が存在しない、Supabase移行後に初めて可能になる) |
| 完全削除とアクセス停止の分離 | ❌ 現状は承認待ちメンバーの「却下」だけが唯一の削除機能で、これは即ハード削除。在籍済み家庭を止める/消す機能はどちらもない |
| 過去データを壊さず退部・卒部を扱う | ❌ 選手の在籍状態・学年・年度・在籍期間を表すフィールドが一切ない |
| 論理削除(soft delete) | ❌ 未導入。今のところ配列から要素を消す=物理削除しかない |

### 1-6. 動画・ライブ配信(YouTube)

| 項目 | 現状 |
|---|---|
| 動画ファイルを保存しない方針 | ✅ 既に一致(URL文字列のみ) |
| YouTube動画ID・タイトル・Q情報・配信日時・公開状態などのメタデータ管理 | △ 現状は `videoUrl` / `liveUrl` / `photoUrl` の3つのURL文字列のみ。動画IDの抽出や、Qごとの管理、公開状態などの構造化はまだない(将来拡張の余地は十分ある) |
| 「YouTube限定公開=完全なアクセス制御ではない」の明記 | ❌ `SPEC.md` にはまだ明記されていない(口頭の方針としては一致している) |

### 1-7. セキュリティ / Supabase RLS / クラブ単位のデータ分離

| 項目 | 現状 |
|---|---|
| Supabase Auth / RLS | ❌ 未着手(方針としては合意済み、`SPEC.md` にも記載あり) |
| クラブ単位のデータ分離(将来のマルチテナント) | ❌ `club` に相当するIDや概念がどこにもない。現状は「志免南ワイルドキャッツ専用」の作り |
| 家庭単位のアクセス制御 | ❌ 上記の通り、家庭とログインユーザーが同一概念のため、家庭単位でのアクセス制御の設計自体がまだできていない |

### 1-8. お知らせ・通知

| 項目 | 現状 |
|---|---|
| 既読・未読管理 | ❌ 未実装(`announcements[]` に既読情報を持つ仕組みはない) |
| 将来のプッシュ通知に備えた設計 | △ 特に通知を妨げる設計にはなっていないが、意識して作られてもいない |

---

## 2. 表:A〜E分類

| 分類 | 項目 |
|---|---|
| **A. 現在すでに対応できているもの** | ・1家庭に複数の選手 ・「基本ロール+追加権限」という権限の考え方(配車管理者/会計管理者は実装済み) ・選手本人のログイン必須化なし ・動画ファイル自体を保存しない方針 |
| **B. 現在の仕様と矛盾する可能性があるもの** | ~~ライブ配信を「全員に開放」する現行方針 vs 「追加権限で絞る」新方針~~ → **2026-08-28に「追加権限化」で解決・実装済み** ・「1ログイン=1家庭」という現在の認証モデル vs 「保護者ごとに別ログイン」という新要望 → Supabase移行時に`families`/`users`分離で解決予定(当面は1家庭1ログイン運用) ・`familyMembers`(選手ラベル)と `players[]` が名前だけで紐付いており、IDでの一意な連携がない |
| **C. Supabase移行前に必ず決めるべきもの** | ・家庭(family)・ログインユーザー(user)・選手(player)・家族同乗者(family_rider)を別テーブルとして分離する設計 ・配車を「人数ベース」から「個人(乗車者)ベース」に置き換えるテーブル設計 ・基本ロール(管理者/コーチ/保護者/選手)の値の見直しと追加権限(配車/会計/配信)の最終形 ・アカウント状態(在籍中/停止/退部/卒部)の状態遷移設計 ・年度・学年・在籍期間フィールドの設計 ・完全削除とアクセス停止(論理削除)を分離する設計 ・すべての主要テーブルに `club_id` 相当の列を用意しておく(将来のマルチクラブ対応の保険) ・RLSポリシーの設計方針(誰が・どのテーブルの・どの行にアクセスできるか) |
| **D. Supabase移行後でも問題ないもの(=移行とセットで実現されるもの)** | ・アカウント停止のサーバー側強制(RLSでの拒否) ・家庭単位のデータ分離の実際の強制 ・Service Role Key/APIキー等の秘匿、環境変数管理 ・入力値バリデーションのサーバー側実装 |
| **E. 将来的な機能として後回しにできるもの** | ・車両(vehicle)を独立した持ち物として毎回登録し直さずに済むようにする機能 ・YouTube動画のメタデータ拡張(動画ID抽出・Qごとの管理・公開状態・配信日時) ・お知らせの既読/未読管理 ・プッシュ通知 ・複数クラブでの実運用(マルチテナントUI) |

---

## 3. 3段階の優先度整理

### 🟢 今すぐ変更してよいもの(ドキュメントのみ・軽微)

- `SPEC.md` に「YouTube限定公開は完全なアクセス制御ではない」という注意書きを明記する。
- `SPEC.md` に、今回整理した「家庭/保護者/選手/家族同乗者/乗車者」の概念定義を追記する(**設計の言葉の定義を先に揃えておくことで、後のSupabase移行での手戻りを防ぐ**ため)。
- 今回の分析結果(本ドキュメント)をベースに、今後のDB設計方針を`SPEC.md`に追記する。

これらは**コードは一切変更せず**、仕様書の記述を増やすだけなので、今すぐ着手して問題ありません。

### 🟡 Supabase移行前に確定させておくべきもの(設計判断が必要・実装はまだしない)

1. **家庭・ログインユーザー・選手・家族同乗者を独立した概念として設計し直す**(現状の `members`+`familyMembers` の一体構造から分離)。
2. **配車を人数ベースから個人(乗車者)ベースに設計し直す**。
3. **権限モデルの最終形を決める**(基本ロールの値、動画・ライブ配信を追加権限にするかどうかをユーザーと相談して確定)。
4. **アカウント状態(在籍中/利用停止/退部/卒部)と論理削除の設計**。
5. **年度・学年・在籍期間の持ち方**。
6. **`club_id` を将来のために各テーブルに予約しておくかどうかの判断**(実装は不要、設計だけ)。
7. **RLSポリシーの大枠**(「家庭Aの保護者は家庭Bのデータを一切取得できない」を、どのテーブル・どの列で担保するか)。

これらは**今のプロトタイプ(localStorage版)のコードを今すぐ書き換える必要はありません**が、Supabaseの実際のテーブル設計に入る前に、方針を決めておかないと手戻りが大きくなる部分です。

### 🔵 今は変更せず後回しでよいもの

- 車両(vehicle)の独立管理。
- YouTube動画メタデータの拡張(動画ID/Qごと管理/公開状態など)。
- お知らせの既読・未読管理。
- プッシュ通知。
- 複数クラブでの実運用対応。

---

## 4. ユーザーに確認したい点(要判断) → 2026-08-28に両方確定

1. **ライブ配信の権限**: → **追加権限化(絞る)方針で確定**。配車管理・会計管理と同じ「基本ロール+追加権限」の考え方で `canManageLive` を新設(BANDアプリの権限モデルを参考)。プロトタイプ側(`index.html`)にも先行実装済み。
2. **保護者ごとの別ログイン**: → **Supabase移行時に `families`/`users` を分離したうえで、当面は1家庭1ログインのまま運用する**方針で確定(運用のしやすさを優先)。詳細は `SUPABASE_MIGRATION_PLAN.md` を参照。

---

以上、分析・設計提案まで。上記2点はコードにも反映済み(ライブ配信の権限化)、または `SUPABASE_MIGRATION_PLAN.md` に設計として反映済みです。
