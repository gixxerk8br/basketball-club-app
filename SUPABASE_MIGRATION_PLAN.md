# Supabase移行計画(2026-08-27、全フェーズ完了: 2026-08-28)

**進捗**: フェーズ2〜6(認証・スケジュール/出欠・配車・試合結果/スタッツ・会費/会計)すべて実装・動作確認まで完了し、Supabaseへの移行が完了しました。詳細は本文末尾の「フェーズ2 完了報告」「フェーズ6(最終)完了報告」を参照してください。

作成目的: プロトタイプ(localStorageのみ)から、本番運用を見据えたSupabaseバックエンドへ移行するための設計・進め方をまとめる。

**このドキュメントは計画書。まだSupabaseプロジェクトの作成・コードの実装は行っていません。** ユーザーの確認後、段階的に実装します。

---

## 1. なぜSupabaseか / 無料枠で足りるか

グローバルのルール(無料運用の原則)に沿って、導入前に整理します。

### なぜ必要か

現在のプロトタイプは、データがブラウザの `localStorage` にしか保存されないため、**家族間でデータが共有されません**(お父さんのスマホで入力した出欠が、お母さんのスマホには表示されない)。これがアプリの実用化における最大の制約であり、解消するにはサーバー側にデータベースを置く必要があります。

### なぜSupabaseか

- PostgreSQL(本格的なデータベース)+ 認証(Auth)+ アクセス制御(Row Level Security)が最初からセットになっている
- 無料枠があり、小規模なクラブ運営の規模であれば十分に収まる見込み
- これまでの設計方針(`SPEC.md` に記載)ともともと合意済み

### 無料枠(Free Plan)で足りるか(2026年8月時点で確認)

| 項目 | 無料枠の内容 |
|---|---|
| データベース容量 | 500MB |
| ファイルストレージ | 1GB |
| データ転送量(egress) | 月5GB |
| 月間アクティブユーザー数 | 50,000人まで |
| プロジェクト数 | 無料プロジェクトは2つまで |
| 注意点 | **1週間アクセスが無いとプロジェクトが自動的に一時停止**します(手動で再開可能)。バックアップ機能・SLAはなし |

出典: [Supabase Pricing 2026 (UI Bakery)](https://uibakery.io/blog/supabase-pricing)、[Supabase Free Tier Limits 2026 (AI Agency Plus)](https://aiagencyplus.com/supabase-free-tier-limits/)

**このクラブの規模(数十家庭程度、写真・動画は保存しない方針)であれば、データ容量・アクセス数は無料枠で十分に収まります。**

唯一の注意点は「1週間アクセスがないと自動一時停止する」点です。オフシーズン等でしばらく誰もログインしない期間があると、次に開いたときに数秒〜数十秒の再起動待ちが発生する可能性があります(データが消えるわけではありません)。実害が出るようであれば、後から検討します。

**費用は今のところ発生しない見込みです。** 万が一、容量や転送量が無料枠を超えそうな場合は、事前に(1)なぜ必要か (2)無料枠で何が不足するか (3)概算費用 (4)無料の代替案 をご説明したうえで、許可をいただいてから対応します。

---

## 2. Supabaseプロジェクトの作成について(私にはできない作業)

Supabaseへのアカウント登録・プロジェクト作成は、私(Claude)からは実行できません(アカウント作成はセキュリティ上の理由で行わない方針のためです)。**お手数ですが、以下はご自身で行っていただく必要があります。**

1. [supabase.com](https://supabase.com/) でアカウント登録(GitHubアカウント等でのログインも可)
2. 新規プロジェクトを作成(リージョンは日本に近い場所、例: Tokyo (ap-northeast-1) を推奨)
3. プロジェクトの「Project URL」と「anon / public key」を控える

これらを`.env.local` のような環境変数ファイルに設定していただければ、以降のコード実装は私が進めます。**「Service Role Key」は絶対にコードへ組み込まないので、共有していただく必要はありません。**

> 補足: 「anon / public key」は、Supabaseの仕組み上ブラウザ側のコードに含める前提のキーです(RLSで保護されるため、これ自体は秘密情報として扱われません)。一方「Service Role Key」はRLSを無視できる強い権限を持つ鍵のため、絶対にブラウザ側へ出してはいけません。この区別を今後も徹底します。

---

## 3. データベース設計(テーブル案)

これまでの分析(`SPEC_REVIEW_2026-08.md`)を踏まえ、以下のテーブル構成を提案します。**配列で持っていたデータ(選手・家族同乗者・配車回答など)を、それぞれ独立したテーブルに分解**します。これにより、Supabaseの行単位アクセス制御(RLS)が効くようになります。

```
clubs                … クラブ(将来の複数クラブ対応の土台。今は1行のみ)
families              … 家庭(在籍状態: active / suspended)
users                 … ログインアカウント(Supabase Authと1:1、familyに属する)
family_members        … 家族同乗者(クラブ非所属のきょうだい等。familyに属する)
players               … 選手ロースター(familyに属する。在籍状態・背番号など)
schedule_days         … 日程(1日1件。資料materials・対戦相手opponentsはJSON列)
attendance            … 出欠(schedule_day × user で1行、name文字列キーをやめてuser_idに)
carpool_responses     … 配車の可否回答(schedule_day × user で1行)
carpool_participants  … 配車の乗車者(選手/家族同乗者/スポット同乗者を1行ずつ管理。誰の車に乗るかもここで持つ)
games                 … 試合記録(動画・ライブ配信URL、クォーター得点、個人スタッツはJSON列)
announcements         … お知らせ
bylaws_articles       … クラブ規約
finance_settings      … 会費設定
fee_payments          … 会費支払い状況
finance_entries       … 会計帳簿
```

### 設計上のポイント

- **すべてのテーブルに `club_id` 列を持たせます**(今は1クラブのみですが、将来複数クラブに対応する際の土台として)。
- **`carpool_participants` が今回の大きな改善点**です。今のプロトタイプでは「乗車者ID→運転者ID」という対応をJSONで持っていますが、Supabase移行時はこれを正式なテーブルにします。1行=1人分の乗車情報になるため、「誰が・どの車に・その日乗るか」をデータベース側で正確に管理できます。
- **`attendance` を `name`(文字列)ではなく `user_id` で管理し直します**。現在のプロトタイプは出欠を氏名の文字列でキーにしており、改名や同姓同名に弱い作りでした。この移行を機に直します(挙動は変わりません)。
- **写真・動画はURL/メタデータのみ**(`games.video_url` 等の文字列列)。動画ファイル自体はSupabaseに保存しません(方針どおり)。
- **個人情報を最小限に**: 生年月日は持ちません。家族同乗者(`family_members`)は氏名と続柄のみとし、それ以外の項目は追加しません。

---

## 4. アクセス制御(Row Level Security)の方針

すべてのテーブルでRLSを有効にします。大まかな考え方は以下のとおりです。

| 区分 | 誰が読めるか | 誰が書けるか |
|---|---|---|
| 日程・お知らせ・試合結果・規約・選手ロースター(氏名程度) | 同じクラブの承認済み・停止されていない会員全員 | 管理者のみ |
| 出欠・配車の回答/乗車者情報 | 同じクラブの会員全員(配車は他家庭の状況を見る必要があるため) | 自分の家庭の分だけ本人が書ける。配車の組み立て(運転者の割当)は配車管理者・管理者のみ |
| 動画・ライブ配信のURL(`games.video_url`/`live_url`/`is_live`) | 同じクラブの会員全員(視聴は誰でも可) | 配信管理者(`canManageLive`)・管理者のみ |
| 会員のメールアドレス・家族同乗者の詳細 | 本人の家庭のみ(管理者は除く) | 本人の家庭のみ |
| 会費・会計 | 本人の家庭分は本人のみ閲覧可。全体は会計担当・管理者のみ | 会計担当・管理者のみ |
| 家庭のアクセス状態(利用停止等)・権限フラグ | 管理者のみ | 管理者のみ |

**最重要ポイント**: 「家庭Aの保護者が、URLを直接操作するなどして家庭Bの個人情報を取得できてしまう」ことがないよう、すべてのポリシーで `family_id` による絞り込みを行います。また、`families.status = 'suspended'`(利用停止)の家庭は、**データベース側のポリシーでも**すべての読み書きを拒否します(現状のプロトタイプはブラウザ側のチェックのみでしたが、ここが実質的に補われます)。

**実装時の注意点(動画・ライブ配信)**: `games` テーブルは対戦相手・得点・スタッツなど大部分が管理者のみ書き込み可能ですが、`video_url`/`live_url`/`is_live` の3列だけは配信管理者にも書き込みを許す必要があります。SupabaseのRLSは行単位の制御が基本で、同じテーブル内で列ごとに異なる書き込み権限を設定するには工夫が要ります(例: 更新後の値をチェックするポリシー関数を書く、またはこの3列だけ `game_live_settings` という別テーブルに分離する)。スキーマ作成時に具体的な実装方法を決めます。

---

## 5. 認証方式

- Supabase Auth のメールアドレス+パスワード認証を基本とします(現在のモック認証を置き換え)。
- Googleログインは、コード上に「デモ版のため無効」という案内が既にありますが、**今回は導入しません**(新しい外部サービス連携にあたるため、別途ご相談してから進めます)。
- パスワードの暗号化・再設定メール送信などは、すべてSupabase Auth標準機能に任せます(自前実装はしません)。

---

## 6. 移行の進め方(段階的に実施)

一度にすべてを切り替えるのはリスクが大きいため、次の順番で進めることを提案します。各段階の後、動作確認していただいてから次に進みます。

1. **Supabaseプロジェクトの準備**(ユーザー作業): アカウント登録・プロジェクト作成・URLとanon keyの取得
2. **認証だけ疎通確認**: ログイン/サインアップ/ログアウトをSupabase Authに置き換え、承認待ち状態などの基本動作を確認
3. **スキーマ作成 + RLSポリシー**: 上記テーブルをSupabase上に作成し、テストデータでポリシーが正しく機能するか確認(他家庭のデータが見えないこと等を重点的に確認)
4. **画面ごとに段階移行**: スケジュール→出欠→配車→試合結果・スタッツ→会計、の順にlocalStorageへの読み書きをSupabase呼び出しに置き換え
5. **既存デモデータの扱いを決定**: プロトタイプのデモデータは本番投入せず、実際のクラブ情報を入力し直す想定です
6. **本番公開**: 公開前に、RLSが有効であること・秘密情報がコードに含まれていないことを最終確認

**既存機能を壊さないよう、1つの画面が完全に移行できるまでは、その画面はlocalStorage版のまま残す**、という進め方も可能です。ここは実装時にご相談しながら決めます。

---

## 7. 決定事項(2026-08-28確定)

1. **保護者ごとの別ログイン**: 「家庭とログインユーザーは別テーブルにしつつ、当面は1家庭1ログインで運用する」という方針(本計画の `families`/`users` 分離設計)で確定。運用のしやすさを優先。
2. **ライブ配信の権限**: 配車・会計と同じ「追加権限」方式に変更することが確定(BANDアプリの権限モデルを参考)。`canManageLive` を持つ会員(管理者は自動的に保有)だけが動画・ライブ配信URLの投稿・編集・削除・配信開始/終了を行える。**この変更はSupabase移行を待たず、プロトタイプ側(`index.html`)に先行実装済み**(`isLiveManager` getter、管理者ダッシュボードの🔑役割・権限の設定に「🎥 配信管理」列を追加)。Supabase移行時は、`games` テーブルの `video_url`/`live_url`/`is_live` 列の書き込みポリシーを「`canManageLive` または管理者」に限定する形でこの方針を引き継ぐ(視聴・閲覧は引き続き全会員可)。

---

## フェーズ2 完了報告(2026-08-28)

`clubs` / `families` / `users` / `players` / `family_members` の5テーブルを作成し、RLSを有効化。ログイン・新規登録・ログアウト・管理者による承認/却下/権限付与/利用停止/完全削除を、実際のSupabaseプロジェクトに接続して動作確認まで完了しました。

**実装したファイル**:
- [supabase/phase2_schema.sql](supabase/phase2_schema.sql) — テーブル定義・RLSポリシー(Supabase SQL Editorで実行済み。再実行しても安全な形にしてあります)
- `index.html` — Supabase JS SDKの追加、`login()`/`signup()`/`logout()`/`init()`、会員承認・権限管理・選手ロースター管理をSupabase連携に置き換え

**実装中に見つかり、その場で修正した問題**:
1. プロジェクト作成時に「Automatically expose new tables」をオフにしたため、テーブルへのAPI権限(GRANT)が不足していた → SQLにGRANT文を追加
2. 家庭(families)登録直後に内容を読み返す処理が、RLS上「承認前は自分の家庭情報も見れない」ルールに阻まれていた → IDをブラウザ側で先に生成し、読み返さない設計に変更
3. 選手(players)の登録が「管理者のみ」の権限になっており、サインアップ時に選手登録ができなかった → 「自分の家庭の分ならOK」に修正
4. Supabaseの「Confirm email」(メール確認)がオンだと、今の実装ではサインアップが完結しないうえ無料枠のメール送信数制限にすぐ達するため、オフに変更(管理者の承認が実質的なアクセス制御のため、実害は小さいと判断)

**動作確認済みの項目**:
- サインアップ(保護者・選手・家族同乗者の登録を含む)→ 承認待ち状態でのログイン拒否
- 管理者による承認待ち一覧の表示・承認
- 管理者による役割・権限(配車管理/会計担当/配信管理)の付与
- ログイン後、次回ログイン時のメールアドレス自動入力(パスワードは保存せず、ブラウザ自体のパスワード保存機能に委ねる設計)

**今回あえて広げたアクセス範囲(ご承知おきください)**: 当初の設計では会員のメールアドレスは「本人の家庭のみ閲覧可」としていましたが、実装時に「承認済み会員なら誰でも他の家庭の名前を閲覧できる」設計(`users`/`families`/`family_members`/`players`の閲覧ポリシー)に広げました。配車機能が「他の家庭の名前」を画面に表示する必要があり、家庭ごとに限定すると配車・出欠の表示が壊れてしまうためです。停止中・承認待ちのアカウントには一切見えないため悪用リスクは低いですが、メールアドレスまで会員全員に見える点は、気になるようであれば別途絞り込みも可能です。

**未実施(次のフェーズ以降)**:
- スケジュール・配車・試合結果・スタッツ・会計は引き続き`localStorage`のまま(端末ごとに別々)。次のフェーズでこれらをSupabaseへ移行する
- 「完全削除」はアプリのプロフィール削除までで、Supabase Auth側の認証エントリ自体の削除にはサーバー側処理(Edge Function等、service_role権限が必要)が別途必要

---

## フェーズ3〜5 完了報告(2026-08-28、概要)

フェーズ2に続き、以下も実際のSupabaseプロジェクトに接続して実装・動作確認まで完了しています(詳細な設計判断は各SQLファイルのコメントを参照)。

- **フェーズ3(スケジュール・出欠)**: `schedule_days` / `attendance` の2テーブルを追加。出欠は氏名文字列ではなく `user_id` で管理する設計に変更(意味は変わりません)。[supabase/phase3_schedule_schema.sql](supabase/phase3_schedule_schema.sql)
- **フェーズ4(配車)**: `carpool_responses` / `carpool_assignments` / `carpool_spot_riders` の3テーブルを追加。「配車の可否は本人の家庭のみ、割り当ての組み立ては配車管理者のみ」という権限をテーブル単位のRLSで再現。[supabase/phase4_carpool_schema.sql](supabase/phase4_carpool_schema.sql)
- **フェーズ5(試合結果・スタッツ)**: `games`(基本情報・管理者のみ編集)/ `game_media`(動画・ライブURL・配信管理者のみ編集)/ `game_live_stats`(スタッツ・承認済み会員なら誰でも記録)の3テーブルに分離し、既存の権限モデルをそのままRLSに落とし込み。[supabase/phase5_games_schema.sql](supabase/phase5_games_schema.sql)

---

## フェーズ6(最終)完了報告(2026-08-28)

`finance_settings`(月会費設定)/ `fee_payments`(会費納付記録)/ `finance_entries`(収入・支出の帳簿)の3テーブルを作成し、RLSを有効化。会計画面自体が「会計担当者(`canManageFinance`)または管理者」しか開けない仕組みのため、3テーブルとも同一の権限(`is_finance_manager()`)に統一するシンプルな設計にしました。

**実装したファイル**:
- [supabase/phase6_finance_schema.sql](supabase/phase6_finance_schema.sql) — テーブル定義・RLSポリシー(Supabase SQL Editorで実行済み)
- `index.html` — `fetchFinanceFromSupabase()` / `updateMonthlyFee()` / `markFeePaid()` / `unmarkFeePaid()` / `addFinanceEntry()` / `deleteFinanceEntry()` をSupabase連携に置き換え

**実装中に見つかり、その場で修正した問題**:
- `unmarkFeePaid()`(会費の取消)で、先に `fee_payments` を削除すると外部キーの `on delete set null` が働いて `finance_entries.linked_fee_payment_id` が先にnullになってしまい、直後に行っていた「連動する帳簿記録の削除」が対象を見失い、帳簿に取消し忘れの記録が残り続けるバグを発見。削除の順序を「先に帳簿(`finance_entries`)→ 次に納付記録(`fee_payments`)」に入れ替えて修正し、DB上に痕跡が残らないことを確認しました。

**動作確認済みの項目**:
- 月会費の設定変更(3000円→3500円)→ リロードしても値が残ることを確認
- 会費の「納付済みにする」→ 納付記録と帳簿(収入)が連動して作成されることを確認(実際の画面操作でも確認)
- 会費の「取消」→ 納付記録・帳簿の両方がきれいに削除され、孤立したデータが残らないことを確認
- 帳簿への手動記録の追加・削除
- コンソールにエラーが出ないこと

**テストデータの後片付け**: 今回作成したテストアカウント(`claude-finance-verify@gmail.com`)に加え、フェーズ3〜5で残っていた未削除のテストアカウント3件(`claude-sched-verify@gmail.com` / `claude-carpool-verify@gmail.com` / `claude-games-verify@gmail.com`)もあわせて削除しました。いずれもアプリの `users` テーブル上のプロフィール行と、紐づく `families` 行のみの削除で、Supabase Auth側の認証エントリ自体(ログイン不可・承認待ちのまま残る、実害なし)とご本人(吉田様)のアカウントには影響していません。

### これで移行は完了です

`persist()`(localStorageへの保存)は、これで **お知らせ(`announcements`)のみ** になりました。会員情報・スケジュール・出欠・配車・試合結果・スタッツ・会費・会計は、すべてSupabase(PostgreSQL + Auth + RLS)が正のデータとなり、家族間でのデータ共有(お父さんが入力した出欠がお母さんの画面にも反映される、等)が実現しています。

---

以上で、`SUPABASE_MIGRATION_PLAN.md` に記載した移行計画はすべて完了しました。
